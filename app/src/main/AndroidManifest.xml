<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".perfil"></activity>
        <activity android:name=".resultados" />
        <activity android:name=".juego" />
        <activity android:name=".avatar" />
        <activity android:name=".registrarse" />
        <activity android:name=".iniciarsesion" />
        <activity android:name=".instrucciones" />
        <activity android:name=".mundos" />
        <activity android:name=".mundoAtencion" />
        <activity android:name=".mundoMemoria" />
        <activity android:name=".informacion" />
        <activity android:name=".ranking" />
        <activity android:name=".comienza" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>